import 'dart:math';

void main() {
  int quantity = 100;
  double price = 4.99;

  // quantity = quantity + 50;
  // quantity *= 50;
  // quantity += 50;
  // quantity -= 50;

  // quantity = quantity + 1;
  // quantity++;
  print(min(100, 300));
  // print(min(100, 300)); mencetak perbandingan angka yang lebih kecil dalam hal ini 100
  // print(max(100, 300)); mencetak perbandingan angka yang lebih besar dalam hal ini 300
}
