void main() {
  String name = "John";
  int age = 20;
  bool isRegisteredVoter = false;

  print("There once a named ${name}");
}

/*
Dart when it supports type inference, interestingly we declare
variable without doing initialization, variable will have dynamic type.
Since a dynamic variable can contain any type, it is not
there is a problem if we change the value in it.
 */